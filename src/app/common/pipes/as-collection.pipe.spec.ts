import { AsCollectionPipe } from './as-collection.pipe';

describe('AsCollectionPipe', () => {
  it('create an instance', () => {
    const pipe = new AsCollectionPipe();
    expect(pipe).toBeTruthy();
  });
});
